# دليل البدء السريع - منصة سلمى التعليمية 🚀

## ⚡ الإعداد السريع (5 دقائق)

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. إعداد المفاتيح

قومي بتعديل ملف `.env` وأضيفي مفتاح EmailJS العام الخاص بك:

```env
VITE_EMAILJS_PUBLIC_KEY=ضعي_مفتاحك_هنا
```

> **ملاحظة:** باقي الإعدادات موجودة بالفعل في الملف!

### 3. تشغيل المشروع
```bash
npm run dev
```

افتحي المتصفح على: `http://localhost:5173`

## 🎯 الإعدادات المطلوبة (مرة واحدة)

### أ) إعداد Supabase (10 دقائق)
راجعي الملف: [`DATABASE_SETUP.md`](./DATABASE_SETUP.md)

**الخطوات الأساسية:**
1. سجّلي في [Supabase](https://supabase.com)
2. أنشئي مشروع جديد
3. نفّذي السكربتات SQL من ملف `DATABASE_SETUP.md`
4. احفظي الـ URL والـ anon key في ملف `.env`

### ب) إعداد EmailJS (5 دقائق)
راجعي الملف: [`EMAILJS_SETUP.md`](./EMAILJS_SETUP.md)

**الخطوات الأساسية:**
1. سجّلي في [EmailJS](https://www.emailjs.com)
2. أضيفي خدمة بريد (Gmail مثلاً)
3. أنشئي قالبين: واحد للمعلمات وواحد للطالبات
4. احفظي المفاتيح في ملف `.env`

## 📝 اختبار التسجيل

### تسجيل معلمة:
1. افتحي `/register`
2. اختاري "معلمة"
3. املئي البيانات
4. ستحصلين على كود فصل (مثال: `ABC123`)
5. سيصلك إيميل ترحيبي

### تسجيل طالبة:
1. افتحي `/register`
2. اختاري "طالبة"
3. املئي البيانات
4. أدخلي كود المعلمة
5. سيصلك إيميل ترحيبي

## 🔧 الأوامر المتاحة

```bash
# تشغيل المشروع (Development)
npm run dev

# بناء المشروع (Production)
npm run build

# معاينة البناء
npm run preview

# تشغيل على شبكة محلية
npm run dev -- --host
```

## 📱 الوصول من الموبايل

### على نفس الشبكة:
1. شغّلي المشروع بـ `npm run dev -- --host`
2. ستظهر لك IP address مثل: `http://192.168.1.5:5173`
3. افتحي هذا الرابط من الموبايل

### كـ PWA (تطبيق):
1. افتحي المنصة من المتصفح
2. انقري على "Add to Home Screen"
3. ستعمل كتطبيق مستقل!

## 🎨 التخصيص

### تغيير الألوان:
افتحي `src/index.css` وعدّلي المتغيرات:
```css
:root {
  --bg-primary: #7DBECF;      /* اللون الأساسي */
  --main-element: #926A2B;    /* لون العناصر */
  --accent-spark: #FD0000;    /* لون التمييز */
  /* ... */
}
```

### تغيير الشعار:
استبدلي الملف `public/logo.png` بشعارك

### تغيير الخطوط:
عدّلي رابط Google Fonts في `index.html`

## 🐛 حل المشاكل الشائعة

### المشروع لا يعمل:
```bash
# امسحي node_modules وأعيدي التثبيت
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### البريد الإلكتروني لا يُرسل:
- تحققي من صحة مفتاح EmailJS في `.env`
- راجعي حصة الإرسال الشهرية (200 مجاناً)
- افتحي Console في المتصفح للأخطاء

### قاعدة البيانات لا تستجيب:
- تحققي من صحة SUPABASE_URL و ANON_KEY
- تأكدي من إنشاء الجداول بشكل صحيح
- راجعي سياسات RLS في Supabase

### الصور لا تظهر:
- تأكدي من وجود ملف `public/logo.png`
- شغّلي المشروع من المجلد الصحيح

## 📚 الملفات المهمة

- `src/App.jsx` - نقطة البداية + التوجيه
- `src/pages/HomePage.jsx` - الصفحة الرئيسية
- `src/pages/RegisterPage.jsx` - صفحة التسجيل
- `src/index.css` - جميع الأنماط
- `.env` - المفاتيح والإعدادات

## 🎓 الخطوات التالية

بعد إتمام الإعداد، يمكنك:

1. ✅ إضافة صفحة تسجيل الدخول
2. ✅ لوحة تحكم المعلمة
3. ✅ لوحة تحكم الطالبة
4. ✅ صفحات الألعاب والتحديات
5. ✅ نظام النقاط والإنجازات

## 💡 نصائح

- احفظي ملف `.env` في مكان آمن
- لا ترفعي `.env` إلى GitHub
- استخدمي مفاتيح مختلفة للتطوير والإنتاج
- اعملي نسخ احتياطية من قاعدة البيانات

## 🤝 الدعم

إذا واجهتِ أي مشكلة:
1. راجعي ملفات التوثيق (`README.md`, `DATABASE_SETUP.md`, `EMAILJS_SETUP.md`)
2. افحصي Console في المتصفح
3. راجعي logs في Terminal

---

**بالتوفيق! 🌟**
